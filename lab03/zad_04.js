function check (table) {
    const odp = {}
    for (let i = 0; i <= length.table; i++) {
        let x = table[i]
        for (let j = 0; j <= length.table; j++) {
            if (table[i] === table[j])
        } 
    }
}